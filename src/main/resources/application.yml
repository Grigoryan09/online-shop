spring:
  application:
    name: online-shop
  datasource:
    url: jdbc:mysql://localhost:3306/online_shop
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-request-size: 10MB
  messages:
    basename: messages
    encoding: utf-8
    fallback-to-system-locale: false
online:
  shop:
    upload:
      image:
        directory:
          path: C:\\Users\\MSI\\IdeaProjects\\online-shop\\upload_images\\